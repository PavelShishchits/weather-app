<!doctype html>
<html lang="en">
{{>head}}
    <body>
        <div class="site-wrap">
            {{>header}}
            <div class="content-wrap">
                <p>{{ message }}</p>
            </div>
            {{>footer}}
        </div>
        <script src="/js/app.js"></script>
    </body>
</html>